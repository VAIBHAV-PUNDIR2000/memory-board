import React from "react";

const EmptyShow = () => {
  return (
    <div
      style={{
        height: "50vh",
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Add data to start off
    </div>
  );
};

export default EmptyShow;
